#include "../Headers/prototype.h"

int AI(int random){
    if (random >= 0 || random < 74){
    // l'ennemi attaque
    }
    if (random >= 75 || random < 101){
    // l'ennemi defend
    }
    return 0;
}